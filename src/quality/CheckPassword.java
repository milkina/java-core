package quality;

public class CheckPassword {

 /*   public boolean checkPassword(String userName, String password) {
        if (userName.equals(getPasswordFromDB(userName)) {
            Session.initialize();
            return true;
        }
        return false;
        //Is employee eligible for benefit
        if (employee.getAge() > 65 && employee.getWorkingYears()>45){
            ...
        }
    }

    public boolean isEmployeeEligibleForBenefit(){
        return employee.getAge() > 65 && employee.getWorkingYears()>45;
    }*/
}
